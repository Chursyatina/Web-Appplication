import { createStyles, makeStyles } from '@material-ui/core/styles';

export const itsEmptyStyles = makeStyles(() =>
  createStyles({
    root: {
      alignItems: 'center',
      alignSelf: 'center',
      alignContent: 'center',
      marginTop: 40,
    },
  }),
);
